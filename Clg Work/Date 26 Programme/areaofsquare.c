#include<stdio.h>
#include<conio.h>

int main ()

{
    int area,length;

    printf("Enter the Length :- ");
    scanf("%d",&length);

    area = length*length;

    printf("Area of Length is :- %d",area);

    return 0;
}